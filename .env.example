# Variáveis para ElevenLabs (já em uso na Vercel)
# VITE_ELEVENLABS_API_KEY=sk-...   (não usado no client — a key fica no Edge Function)
ELEVENLABS_ALFAFA_VOICE_ID=          # voz masculina do Alfafa (ex: "pNInz6obpgDQGcFmaJgB")
# Alice (voz feminina infantil) é hardcoded no /api/tts.ts como "21m00Tcm4TlvDq8ikWAM"

# Supabase (FASE 2 - sync de perfis e progresso)
# Crie um projeto em https://supabase.com 
# Vá em Project Settings > API > "Project API keys" e copie:
# - Project URL  → VITE_SUPABASE_URL
# - anon / public key (nunca o service_role!) → VITE_SUPABASE_ANON_KEY
VITE_SUPABASE_URL=https://sqyhshtfcuilednkfnex.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InNxeWhzaHRmY3VpbGVkbmtmbmV4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODA0MTU3MTgsImV4cCI6MjA5NTk5MTcxOH0.UnVP_Xb2kFrNJf3xXZrgn_JZzS5EF7VjTatJEg1g5_E

# URL pública do app (usada para magic links / redirects do Supabase)
# Em produção Vercel, defina esta variável também.
VITE_SITE_URL=https://jogo-alfabeto-beta.vercel.app

# Dica: use 1Password + op CLI ou vercel env para não hardcodar em produção.

# Depois de criar as tabelas, habilite RLS + políticas:
# - Tabela: children (id, parent_id uuid references auth.users, name, avatar, stars, letter_mastery jsonb, created_at, updated_at)
# - Política: "Users can only access their own children" (parent_id = auth.uid())
